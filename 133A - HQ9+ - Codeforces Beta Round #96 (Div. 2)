#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    getline(cin>>ws,s);
    bool ans=false;
    for( int i=0;i<s.size();i++){
        if((s[i]=='H' || s[i]=='Q' || s[i]=='9') && s[i]>32 && s[i]<127 ){
            ans=true;
            break;
        }
    }
    if(ans){
        cout<<"YES";
    }
    else{
        cout<<"NO";
    }
}
